version: 2

# Set the OS, Python version and other tools you might need
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
    
sphinx:
  configuration: POC/docs/source/conf.py

python:
  install:
    - requirements: POC/docs/source/requirements.txt
